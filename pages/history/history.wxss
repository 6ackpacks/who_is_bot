/* pages/history/history-enhanced.wxss - 增强版判定历史 */

/* ===== 容器 ===== */
.history-container {
  width: 100%;
  min-height: 100vh;
  background: var(--bg-primary, #F9F8F6);
  position: relative;
  overflow: hidden;
}

/* 背景装饰 */
.history-container::before { content: ""; }

/* 赛博朋克网格背景 */
.history-container::after { content: ""; }

.history-scroll {
  width: 100%;
  height: 100vh;
}

/* ===== 页面标题 ===== */
.page-header {
  background: var(--surface-white, #FFFFFF);
  backdrop-filter: blur(20rpx);
  padding: 60rpx 40rpx 40rpx;
  margin-bottom: 20rpx;
  border-bottom: 2rpx solid var(--border-subtle, #E8E6DC);
  position: relative;
  animation: slideInDown 0.6s cubic-bezier(0.16, 1, 0.3, 1);
}

.page-header::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 2rpx;
  background: linear-gradient(90deg, transparent, rgba(0, 212, 255, 0.6), transparent);
}

.header-title {
  display: flex;
  align-items: baseline;
  gap: 12rpx;
  margin-bottom: 16rpx;
}

.title-main {
  font-size: 48rpx;
  font-weight: 700;
  color: #FFFFFF;
  font-family: var(--font-display);
}

.title-highlight {
  font-size: 48rpx;
  font-weight: 700;
  color: var(--accent-clay, #D97757);
  font-family: var(--font-display);
  text-shadow: none;
}

.header-subtitle {
  font-size: 24rpx;
  color: var(--text-muted, #66655F);
  font-family: var(--font-display);
}

/* ===== 加载状态 ===== */
.loading-container {
  width: 100%;
  height: 80vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

.loading-spinner {
  width: 100rpx;
  height: 100rpx;
  border: 8rpx solid var(--bg-secondary, #F0EEE6);
  border-top-color: var(--accent-clay, #D97757);
  border-radius: 50%;
  animation: spin 1s cubic-bezier(0.68, -0.55, 0.265, 1.55) infinite;
  margin-bottom: 40rpx;
  box-shadow: none;
}

.loading-text {
  font-size: 28rpx;
  color: var(--text-main, #1D1C16);
  font-family: var(--font-display);
  animation: pulse 2s ease-in-out infinite;
}

/* ===== 错误状态 ===== */
.error-container {
  width: 100%;
  height: 80vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 0 60rpx;
}

.error-text {
  font-size: 28rpx;
  color: var(--text-main, #1D1C16);
  text-align: center;
  margin-bottom: 40rpx;
  line-height: 1.6;
  font-family: var(--font-display);
}

.retry-btn {
  padding: 24rpx 60rpx;
  background: var(--accent-clay, #D97757);
  color: #FFFFFF;
  border-radius: 50rpx;
  font-size: 28rpx;
  font-weight: 600;
  border: none;
  font-family: var(--font-display);
  box-shadow: 0 4px 6px -1px rgba(0,0,0,0.05);
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.retry-btn::after {
  border: none;
}

.retry-btn:active {
  transform: translateY(2rpx);
  box-shadow: 0 4rpx 12rpx rgba(0, 212, 255, 0.3);
}

/* ===== 历史记录列表 ===== */
.history-list {
  padding: 20rpx 40rpx;
}

/* 空状态 */
.empty-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 120rpx 60rpx;
  animation: fadeIn 0.6s ease-out;
}

.empty-text {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--text-main, #1D1C16);
  margin-bottom: 16rpx;
  font-family: var(--font-display);
}

.empty-hint {
  font-size: 26rpx;
  color: var(--text-muted, #66655F);
  font-family: var(--font-display);
}

/* ===== 历史记录项 ===== */
.history-item {
  background: var(--surface-white, #FFFFFF);
  backdrop-filter: blur(10rpx);
  border-radius: 20rpx;
  padding: 32rpx;
  margin-bottom: 20rpx;
  border: 2rpx solid var(--border-subtle, #E8E6DC);
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05), 0 2px 4px -1px rgba(0, 0, 0, 0.03);
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  animation: slideInLeft 0.5s cubic-bezier(0.16, 1, 0.3, 1);
  position: relative;
  overflow: hidden;
}

/* 为每个项目添加延迟动画 */
.history-item:nth-child(1) { animation-delay: 0.1s; }
.history-item:nth-child(2) { animation-delay: 0.15s; }
.history-item:nth-child(3) { animation-delay: 0.2s; }
.history-item:nth-child(4) { animation-delay: 0.25s; }
.history-item:nth-child(5) { animation-delay: 0.3s; }
.history-item:nth-child(6) { animation-delay: 0.35s; }
.history-item:nth-child(7) { animation-delay: 0.4s; }
.history-item:nth-child(8) { animation-delay: 0.45s; }

.history-item::before { content: ""; }

.history-item:active {
  transform: scale(0.98);
  background: rgba(255, 255, 255, 0.08);
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.3);
}

/* 历史记录头部 */
.history-header {
  display: flex;
  align-items: center;
  gap: 20rpx;
  margin-bottom: 20rpx;
}

.history-type-badge {
  width: 64rpx;
  height: 64rpx;
  background: var(--accent-clay, #D97757);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  box-shadow: 0 4rpx 12rpx rgba(0,0,0,0.1);
}

.history-content {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.history-title {
  font-size: 30rpx;
  font-weight: 600;
  color: #FFFFFF;
  font-family: var(--font-display);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.history-time {
  font-size: 24rpx;
  color: var(--text-muted, #66655F);
  font-family: var(--font-display);
}

/* 判定结果 */
.history-result {
  width: 56rpx;
  height: 56rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  font-size: 28rpx;
}

.result-correct {
  background: linear-gradient(135deg, #10B981 0%, #059669 100%);
  color: #FFFFFF;
  box-shadow: 0 4rpx 12rpx rgba(16, 185, 129, 0.4);
}

.result-wrong {
  background: linear-gradient(135deg, #EF4444 0%, #DC2626 100%);
  color: #FFFFFF;
  box-shadow: 0 4rpx 12rpx rgba(239, 68, 68, 0.4);
}

/* 用户选择 */
.history-choice {
  font-size: 26rpx;
  color: var(--text-muted, #66655F);
  margin-bottom: 12rpx;
  font-family: var(--font-display);
}

.choice-badge {
  display: inline-flex;
  align-items: center;
  gap: 6rpx;
  padding: 6rpx 16rpx;
  border-radius: 12rpx;
  font-size: 24rpx;
  font-weight: 600;
  margin-left: 8rpx;
  font-family: var(--font-display);
}

.choice-ai {
  background: rgba(239, 68, 68, 0.15);
  color: #EF4444;
  border: 1rpx solid rgba(239, 68, 68, 0.3);
}

.choice-human {
  background: var(--bg-secondary, #F0EEE6);
  color: var(--accent-clay, #D97757);
  border: 1rpx solid var(--border-subtle, #E8E6DC);
}

/* 详情提示 */
.detail-hint {
  font-size: 24rpx;
  color: var(--accent-clay, #D97757);
  text-align: right;
  font-family: var(--font-display);
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 4rpx;
}

/* ===== 底部占位 ===== */
.bottom-spacer {
  height: 100rpx;
}

/* ===== 亮色主题样式 ===== */
.theme-light.history-container {
  background: linear-gradient(180deg, #FFFFFF 0%, #F8FAFC 50%, #F1F5F9 100%);
}

.theme-light.history-container::before { content: ""; }

.theme-light.history-container::after { content: ""; }

.theme-light .page-header {
  background: rgba(255, 255, 255, 0.9);
  border-bottom: 2rpx solid var(--border-subtle, #E8E6DC);
}

.theme-light .title-main {
  color: #0F172A;
}

.theme-light .title-highlight {
  color: var(--accent-clay, #D97757);
  text-shadow: none;
}

.theme-light .header-subtitle {
  color: rgba(15, 23, 42, 0.6);
}

.theme-light .loading-text {
  color: rgba(15, 23, 42, 0.7);
}

.theme-light .error-text {
  color: rgba(15, 23, 42, 0.8);
}

.theme-light .empty-text {
  color: rgba(15, 23, 42, 0.7);
}

.theme-light .empty-hint {
  color: rgba(15, 23, 42, 0.5);
}

.theme-light .history-item {
  background: rgba(255, 255, 255, 0.9);
  border: 2rpx solid var(--border-subtle, #E8E6DC);
}

.theme-light .history-type-badge {
  background: var(--accent-clay, #D97757);
  box-shadow: 0 4rpx 12rpx rgba(0, 212, 255, 0.3);
}

.theme-light .history-title {
  color: #0F172A;
}

.theme-light .history-time {
  color: rgba(15, 23, 42, 0.5);
}

.theme-light .history-choice {
  color: rgba(15, 23, 42, 0.6);
}

.theme-light .choice-badge {
  font-weight: 600;
}

.theme-light .choice-ai {
  background: rgba(239, 68, 68, 0.15);
  color: #EF4444;
  border: 1rpx solid rgba(239, 68, 68, 0.3);
}

.theme-light .choice-human {
  background: var(--bg-secondary, #F0EEE6);
  color: var(--accent-clay, #D97757);
  border: 1rpx solid var(--border-subtle, #E8E6DC);
}

.theme-light .detail-hint {
  color: var(--accent-clay, #D97757);
}
